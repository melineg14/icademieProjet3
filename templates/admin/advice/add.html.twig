{# templates/admin/advice/add.html.twig #}
{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un conseil{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" type="text/css" href="{{ asset('css/trix.css') }}">
{% endblock %}

{% block body %}
<div class="container">
    <h3 class="pt-3 h3-admin"><img src="{{ asset('images/add.svg') }}" width="26" alt="Conseil"> Ajouter un conseil</h3>
    {{ form_start(form) }}
        <div class="form-group">
            {{ form_errors(form.title) }}
            {{ form_label(form.title) }} <span id="titleCount"></span>
            {{ form_widget(form.title) }}
        </div>
        <div class="form-group">
            {{ form_errors(form.description) }}
            {{ form_label(form.description) }} <span id="descCount"></span>
            {{ form_widget(form.description) }}
        </div>
        <div class="form-group trix">
            {{ form_errors(form.content) }}
            {{ form_label(form.content) }}
            {{ form_widget(form.content, {'id': 'content'}) }}
            <trix-editor input="content"></trix-editor>
        </div>
        <div class="form-group">
            {{ form_errors(form.imageFile) }}
            {{ form_label(form.imageFile) }}
            {{ form_widget(form.imageFile) }}
        </div>
        <div class="mt-1 float-right">
            <button type="submit" class="btn btn-secondary btn-sm">Ajouter le conseil</button>
        </div>
    {{ form_end(form) }}
    
    
    {# <form>
        <div class="form-group">
            <label for="title">Titre <span id="titleCount"></span></label>
            <input type="text" class="form-control" id="title" name="title" placeholder="Maximum 60 caractères." onkeyup="titleCountUpdate(this.value)">
        </div>
        <div class="form-group">
            <label for="description">Description courte <span id="descCount"></span></label>
            <textarea class="form-control" id="description" name="description" rows="2" placeholder="Maximum 150 caractères." onkeyup="descCountUpdate(this.value)"></textarea>
        </div>
        <div class="form-group trix">
            <label for="content">Description</label>
            <input type="hidden" class="form-control" id="content" name="content">
            <trix-editor input="content"></trix-editor>
        </div>
        <div class="form-group">
            <label for="image">Image</label>
            <input type="file" class="form-control-file" id="image" name="image_name">
        </div>
        <div class="mt-1 float-right">
            <a class="btn btn-sm btn-secondary" href="#">Ajouter le conseil</a>
        </div>
    </form> #}

</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/trix.js') }}"></script>
	<script>
        function titleCountUpdate(str) {
            var lng = str.length;
            document.getElementById("titleCount").innerHTML = '(' + lng + ' sur 60 caractères )';
        }
        function descCountUpdate(str) {
            var lng = str.length;
            document.getElementById("descCount").innerHTML = '(' + lng + ' sur 150 caractères )';
        }
	</script>    
{% endblock %}